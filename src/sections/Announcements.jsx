import React from "react";
import { Container } from "../layout";

const Annoucements = () => {
	return (
		<Container fluid className="bg-primary text-light">
			<p className="pt-2 px-3">Announcements:</p>
			<p className="pb-2 px-5">TBA</p>
		</Container>
	);
};

export default Annoucements;
